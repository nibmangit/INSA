To run the project make sure you should do this

cd ai-assistant

create venv: python -m venv venv
activate it : source venv/bin/activate ... for linux
install requirements: pip install -r requirements.txt
create .env file: and add your api key like this:
Ai_Key="your key"

then run the project and test it